export const _setGeometricLayersVisibilityScale = (e, o, t, i) => { let a = o.map((e => e.type)), m = e.filter((e => -1 !== a.indexOf(e.type))); t.on("zoomend", (function () { for (let e of m) { const i = e.type, a = o.filter((e => e.type === i)); if (1 === a.length) { const o = a[0].visibilityScales; void 0 !== o && o.minZoomLevel !== o.maxZoomLevel && (parseInt(t.getZoom()) > o.minZoomLevel && parseInt(t.getZoom()) <= o.maxZoomLevel ? t.hasLayer(e) || t.addLayer(e) : t.hasLayer(e) && t.removeLayer(e)) } } })) };